# RobotBuddy Test CMakeLists
# This file is used when building tests with Unity framework
# Currently a placeholder - tests will be added as components are developed

# To run tests:
#   cd firmware && idf.py build
#   cd build && ctest --output-on-failure